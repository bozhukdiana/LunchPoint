# LunchPoint

School meal attendance management for Ліцей №1 м. Копичинці.

## Development

1. Copy `.env.example` to `.env` and supply Firebase web configuration values.
2. Install dependencies with `npm install`.
3. Start the local server with `npm run dev`.

The project currently contains configuration and architectural scaffolding only.
