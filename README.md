# LunchPoint

Система обліку харчування учнів.

## Current repository status

The repository currently contains project foundation files only. Application source code has not been added yet.

## Available npm checks

- `npm install`
- `npm run build`
- `npm run lint`
